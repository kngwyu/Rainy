[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
gym = {version = ">=0.10.5", extras = ["atari"]}
numpy = ">=1.0"
opencv-python = ">=3.4"
torch = ">=0.4"
torchvision = ">=0.2.1"
GitPython = ">=2.0"

[dev-packages]
pytest = ">=3.0"

[requires]
python_version = "3.6"
